{
  "extends": "./node_modules/gts/tsconfig-google.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    "esModuleInterop": true,
    "target": "es2022",
    "lib": ["es2022", "dom"],
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts"
  ],
  "//exclude": "test/consumer/probe.ts imports 'tibber-aws' by package name and is compiled only inside the scratch consumer project that test/consumer/run.sh builds.",
  "exclude": [
    "test/consumer"
  ]
}
